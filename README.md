# binary-classifier
classifies whether img is dog or not

FIRST RUN : "loading_data_tensorflow_tutorial_.py"
THEN : "model.py"
LASTLY : "load_trained_model.py"

make sure to modify the chdirs to the relevant dirs
make sure imgs are in same folder as scripts
-can modify this to have their own folder
